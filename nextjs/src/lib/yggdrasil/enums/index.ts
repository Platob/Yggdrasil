// yggdrasil.enums — client-side port (object-oriented). The MIME / media-type
// registry is the most-used cross-language contract; codec lives inside the
// ``MimeType.isCodec`` flag and the ``MediaType`` wrapper.

export { MimeType, MimeTypes, type Magic, type MimeOpts } from "./mimeType";
export { MediaType } from "./mediaType";
