# Backend API URL (used by server-side rewrites in next.config.ts)
API_URL=http://localhost:8100

# Client-side API URL (leave empty to use the /api/* proxy in next.config.ts)
# NEXT_PUBLIC_API_URL=http://localhost:8100
