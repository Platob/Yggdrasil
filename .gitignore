# Rust build artifacts
target/
**/*.rs.bk
*.pdb

# Cargo
# Cargo.lock is committed for binaries, ignored for libraries.
# This crate is a library, so the lockfile is not tracked.
Cargo.lock

# Editor / OS
.DS_Store
*.swp
